{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Live Server 디버깅",
      "url": "http://127.0.0.1:8080", // ✅ Live Server 포트
      "webRoot": "${workspaceFolder}" // ✅ 현재 프로젝트 루트
    }
  ]
}
